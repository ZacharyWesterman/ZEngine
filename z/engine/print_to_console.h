#ifndef PRINT_TO_CONSOLE_H_INCLUDED
#define PRINT_TO_CONSOLE_H_INCLUDED

#include "../core/string.h"

//#include <irrlicht.h>

/*namespace engine
{
    template <typename CHAR>
    void print_to_console(const core::string<CHAR>& scriptText, const irr::IrrlichtDevice* device)
    {
        core::string<char> printable = core::narrow(pure_string(text));

        std::cout << (printable + (CHAR)10).str();
    }
}*/

#endif // PRINT_TO_CONSOLE_H_INCLUDED
